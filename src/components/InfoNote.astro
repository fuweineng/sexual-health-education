---
interface Props {
  label?: string;
  title?: string;
}

const { label, title } = Astro.props;
const heading = label ?? title ?? '温馨提示';
---
<aside class="info-note">
  <strong>{heading}</strong>
  <div><slot /></div>
</aside>
